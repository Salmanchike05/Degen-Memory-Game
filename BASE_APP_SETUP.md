# Base App Mini App Setup Guide

Этот проект соответствует требованиям Base App Mini Apps согласно [официальной документации](https://docs.base.org/mini-apps/quickstart/create-new-miniapp).

## ✅ Что уже настроено:

### 1. Manifest File
- ✅ Файл `.well-known/farcaster.json` создан и настроен
- ✅ Все необходимые поля заполнены
- ⚠️ **Нужно обновить**: заменить `https://your-app-domain.com` на реальный URL после деплоя

### 2. MiniApp SDK
- ✅ SDK подключен через CDN
- ✅ `sdk.actions.ready()` вызывается при загрузке
- ✅ Автоматическое определение пользователя через `sdk.context.user`

### 3. Embed Metadata
- ✅ Метатег `fc:miniapp` добавлен в HTML
- ✅ Метатег `base:app_id` добавлен для верификации
- ⚠️ **Нужно обновить**: заменить `https://your-app-domain.com` на реальный URL

### 4. Account Association
- ✅ Структура готова в manifest
- ⚠️ **Нужно заполнить**: после деплоя через [Base Build Account Association Tool](https://build.base.org)

## 📋 Следующие шаги (после деплоя):

### Шаг 1: Деплой на Vercel
```bash
# Если еще не задеплоено
npm i -g vercel
cd /Users/airm1/Documents/degen-memory-game
vercel
```

### Шаг 2: Обновить URLs в manifest
После деплоя получите URL (например: `https://degen-memory-game.vercel.app`)

1. Откройте `.well-known/farcaster.json`
2. Замените все `https://your-app-domain.com` на ваш реальный URL
3. Обновите `index.html` - метатег `fc:miniapp` (замените URL)

### Шаг 3: Создать Account Association
1. Убедитесь, что manifest доступен: `https://your-url.vercel.app/.well-known/farcaster.json`
2. Отключите Deployment Protection в Vercel:
   - Settings → Deployment Protection → отключить "Vercel Authentication"
3. Перейдите на [Base Build Account Association Tool](https://build.base.org)
4. Вставьте ваш URL и нажмите "Submit"
5. Нажмите "Verify" и следуйте инструкциям
6. Скопируйте `accountAssociation` объект
7. Вставьте его в `.well-known/farcaster.json`

### Шаг 4: Загрузить обновления
```bash
git add .
git commit -m "Update manifest with real URLs and account association"
git push origin main
```

### Шаг 5: Preview
1. Перейдите на [Base Build Preview](https://base.dev/preview)
2. Вставьте ваш URL
3. Проверьте:
   - Embeds (превью приложения)
   - Account association (должна быть зеленая галочка)
   - Metadata (все поля должны быть заполнены)

### Шаг 6: Публикация
Создайте пост в Base App с URL вашего приложения. Приложение появится как launchable mini app!

## 📝 Важные замечания:

- Manifest должен быть доступен по пути `/.well-known/farcaster.json`
- Все URLs должны быть абсолютными (с https://)
- Account association создается только после деплоя
- Используйте Base Build Preview для проверки перед публикацией

## 🎯 Текущий статус:

- ✅ Структура проекта готова
- ✅ SDK интегрирован
- ✅ Manifest настроен
- ⚠️ Нужен деплой и обновление URLs
- ⚠️ Нужна Account Association

Проект готов к деплою! 🚀
